import { Document } from 'coc.nvim';
import Git from './git';
export default class Resolver {
    private git;
    private resolvedRoots;
    private failedDirs;
    constructor(git: Git);
    getGitRoot(fullpath: string): string | null;
    getRootOfDocument(document: Document): string | null;
    resolveGitRoot(doc?: Document): Promise<string>;
}
