import { Document, OutputChannel } from 'coc.nvim';
import Git, { IExecutionResult, SpawnOptions } from './git';
import { Diff } from './types';
interface Decorator {
    changedDecorator: string;
    conflictedDecorator: string;
    stagedDecorator: string;
    untrackedDecorator: string;
}
export default class Repo {
    private git;
    private channel;
    root: string;
    constructor(git: Git, channel: OutputChannel, root: string);
    getHEAD(): Promise<string>;
    hasChanged(): Promise<boolean>;
    getStaged(): Promise<[number, number]>;
    hasUntracked(): Promise<boolean>;
    getStatus(character: string, decorator: Decorator): Promise<string>;
    getDiff(doc: Document): Promise<Diff[]>;
    exec(args: string[], options?: SpawnOptions): Promise<IExecutionResult<string>>;
}
export declare function parseDiff(diffStr: string): Diff[];
export {};
